services:
  website:
    build: .
    container_name: rethink-prod
    restart: unless-stopped
    # Nessuna porta pubblicata sull'host: il sito è raggiungibile solo
    # attraverso Caddy, che lo trova per nome sulla rete Docker
    # rethink-website-prod_default.
    environment:
      - NODE_ENV=production
